name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CI: true

jobs:
  lint_typecheck:
    name: Lint + Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: bash scripts/run-pnpm.sh install --no-frozen-lockfile --prod=false

      - name: Lint
        run: bash scripts/run-pnpm.sh run lint

      - name: Typecheck
        run: bash scripts/run-pnpm.sh run typecheck

      - name: Changelog check
        run: bash scripts/run-pnpm.sh run changelog:check

  unit_tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: bash scripts/run-pnpm.sh install --no-frozen-lockfile --prod=false

      - name: Run unit tests
        run: bash scripts/run-pnpm.sh test

  integration_tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: bash scripts/run-pnpm.sh install --no-frozen-lockfile --prod=false

      - name: Run integration tests
        run: bash scripts/run-pnpm.sh run test:integration

  e2e_tests:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: bash scripts/run-pnpm.sh install --no-frozen-lockfile --prod=false

      - name: Install Playwright browsers
        run: bash scripts/run-pnpm.sh exec playwright install --with-deps chromium

      - name: Run E2E tests
        run: bash scripts/run-pnpm.sh run test:e2e

      - name: Upload Playwright artifacts (failure only)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts-${{ github.run_id }}
          if-no-files-found: ignore
          retention-days: 14
          path: |
            playwright-report
            test-results
