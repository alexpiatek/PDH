{
  "name": "pdh",
  "private": true,
  "scripts": {
    "dev": "concurrently \"PORT=3002 pnpm -C apps/server dev\" \"PORT=3001 NEXT_PUBLIC_WS_URL=ws://localhost:3002 pnpm -C apps/web dev\"",
    "dev:local": "concurrently \"PORT=3002 pnpm -C apps/server dev\" \"PORT=3001 NEXT_PUBLIC_WS_URL=ws://localhost:3002 pnpm -C apps/web dev\"",
    "test": "pnpm -C packages/engine test"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.2",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  }
}
